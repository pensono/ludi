kind Player a X or O
kind Square a Empty or Player

state Board a Grid<Square, 3, 3>
state CurrentPlayer a Player

setup:
    set CurrentPlayer to Random(Player)

action Play(x a Number<1, 3>, y a Number<1, 3>):
    change Board[x, y] to CurrentPlayer

win Horizontal(player a Player):
    when InARow(player, Board, 4, Horizontal)
        or InARow(player, Board, 4, Vertical)
        or InARow(player, Board, 4, Horizontal)
